import * as React from 'react';
import './style.scss';

const Status = (): JSX.Element => (
  <div className="status--wrapper">
    <div className="status--text">Time to work!</div>
  </div>
);

export default Status;
